var documenterSearchIndex = {"docs":
[{"location":"references/#References","page":"References","title":"References","text":"GABM.jl sits at the meeting point of two literatures: the decades-old tradition of agent-based modelling, and the recent work on language models as simulated decision-makers. Citations elsewhere in this manual refer to the entries below.","category":"section"},{"location":"references/#Generative-agents-and-language-model-simulacra","page":"References","title":"Generative agents and language-model simulacra","text":"Park, J. S., O'Brien, J. C., Cai, C. J., Morris, M. R., Liang, P., & Bernstein, M. S. (2023). Generative agents: Interactive simulacra of human behavior. Proceedings of the 36th Annual ACM Symposium on User Interface Software and Technology (UIST '23). The originating paper. Introduces the memory stream, the recency–importance–relevance retrieval function, reflection as the synthesis of higher-level insight, and planning as the decomposition of intentions. The cognitive architecture of GABM.jl — MemoryStream, retrieve, reflect!, plan! — follows this paper directly.\nPark, J. S., Popowski, L., Cai, C. J., Morris, M. R., Liang, P., & Bernstein, M. S. (2022). Social simulacra: Creating populated prototypes for social computing systems. UIST '22. The precursor to the generative- agents paper. Shows that a language model can populate a prototype community with plausible, heterogeneous members — the first argument that personas, not parameter vectors, can specify a synthetic population.\nArgyle, L. P., Busby, E. C., Fulda, N., Gubler, J. R., Rytting, C., & Wingate, D. (2023). Out of one, many: Using language models to simulate human samples. Political Analysis, 31(3), 337–351. Introduces algorithmic fidelity — the degree to which a conditioned language model reproduces the response distribution of a human subpopulation — and the validity questions any generative model of human behaviour must answer.\nAher, G., Arriaga, R. I., & Kalai, A. T. (2023). Using large language models to simulate multiple humans and replicate human subject studies. Proceedings of the 40th International Conference on Machine Learning (ICML '23). Replicates classic behavioural experiments with language-model subjects, and catalogues where the simulation succeeds and where it fails.\nHorton, J. J. (2023). Large language models as simulated economic agents: What can we learn from homo silicus? NBER Working Paper 31122. Argues that language-model agents are a usable substitute for human subjects in exploratory economic experiments, and runs several to make the case.","category":"section"},{"location":"references/#Foundations-of-agent-based-modelling","page":"References","title":"Foundations of agent-based modelling","text":"Schelling, T. C. (1971). Dynamic models of segregation. Journal of Mathematical Sociology, 1(2), 143–186. The canonical agent-based model: mild individual preferences over neighbours produce stark collective segregation. The benchmark against which a generative treatment of the same problem is most naturally compared.\nAxelrod, R. (1984). The Evolution of Cooperation. New York: Basic Books. The iterated prisoner's dilemma as an agent-based model, and the origin of the repeated-cooperation problem used as a running example in this manual.\nEpstein, J. M., & Axtell, R. (1996). Growing Artificial Societies: Social Science from the Bottom Up. Cambridge, MA: MIT Press. The Sugarscape model and the generative programme for social science — explaining a macro-regularity by exhibiting agents whose interaction produces it. GABM.jl borrows the word \"generative\" from this tradition; Park et al. supply the new sense of it.\nEpstein, J. M. (2006). Generative Social Science: Studies in Agent-Based Computational Modeling. Princeton: Princeton University Press. A book-length statement of the generativist methodology and its standards of explanation.\nBonabeau, E. (2002). Agent-based modeling: Methods and techniques for simulating human systems. Proceedings of the National Academy of Sciences, 99(suppl. 3), 7280–7287. A widely cited survey of when an agent-based model is the appropriate tool — and when it is not.\nMacal, C. M., & North, M. J. (2010). Tutorial on agent-based modelling and simulation. Journal of Simulation, 4(3), 151–162. A practical introduction to the components of an agent-based model: agents, environment, scheduling, and data collection — the components Agents.jl supplies.","category":"section"},{"location":"references/#Tooling","page":"References","title":"Tooling","text":"Datseris, G., Vahdati, A. R., & DuBois, T. C. (2022). Agents.jl: A performant and feature-full agent-based modelling software. Simulation, 98(4). The simulation engine GABM.jl builds on. Provides StandardABM, discrete and continuous spaces, schedulers, run!, and data collection; GABM.jl adds only the cognitive layer.\nPromptingTools.jl. Šplíchal, J., and contributors. A provider-agnostic Julia interface to language models — OpenAI, Anthropic, Mistral, Ollama, and others. The package behind PromptingToolsLLM. https://github.com/svilupp/PromptingTools.jl","category":"section"},{"location":"references/#Method-and-critique","page":"References","title":"Method and critique","text":"Gao, C., Lan, X., Li, N., Yuan, Y., Ding, J., Zhou, Z., Xu, F., & Li, Y. (2024). Large language models empowered agent-based modeling and simulation: A survey and perspectives. Humanities and Social Sciences Communications,\nA survey of the emerging field GABM.jl is part of, organised by\napplication domain and by the design choices a generative model must make.\nGrossmann, I., Feinberg, M., Parker, D. C., Christakis, N. A., Tetlock, P. E., & Cunningham, W. A. (2023). AI and the transformation of social science research. Science, 380(6650), 1108–1109. A short, pointed statement of both the promise and the validity hazards of using language models as research subjects — required reading before a generative model is used to make a claim about the world.\nBail, C. A. (2024). Can generative AI improve social science? Proceedings of the National Academy of Sciences, 121(21). Assesses where generative agents can and cannot substitute for human data, and the reproducibility and bias concerns specific to language-model populations — the concerns the deterministic ScriptedLLM backend is designed to help control.","category":"section"},{"location":"api/model/#Models-and-Simulation","page":"Models and Simulation","title":"Models and Simulation","text":"A generative model is an Agents.jl StandardABM carrying a language-model backend and a clock. This page documents the functions that build such a model, populate it, and reach its shared state from inside a step function. The Building Models guide assembles them into a complete simulation.","category":"section"},{"location":"api/model/#Constructing-a-model","page":"Models and Simulation","title":"Constructing a model","text":"generative_abm builds the StandardABM and guarantees it carries an abmllm backend and an abmclock tick counter.","category":"section"},{"location":"api/model/#Accessing-model-state","page":"Models and Simulation","title":"Accessing model state","text":"Inside a step function, these reach the state every generative model shares.","category":"section"},{"location":"api/model/#Step-functions","page":"Models and Simulation","title":"Step functions","text":"clock_step! is the default model_step!; generative_step! is the default agent_step!. A model with custom dynamics replaces the latter and, if it still wants the clock advanced, calls the former.","category":"section"},{"location":"api/model/#See-also","page":"Models and Simulation","title":"See also","text":"Building Models — a complete worked model.\nCognition and LLMs — the operations a step function calls.\nThe Agents.jl documentation — for spaces, schedulers, run!, and data collection, all of which apply to a generative model unchanged.","category":"section"},{"location":"api/model/#GABM.generative_abm","page":"Models and Simulation","title":"GABM.generative_abm","text":"generative_abm(; llm, agent_step!, model_step!, space, properties,\n                 agent_type, kwargs...) -> StandardABM\n\nConstruct an Agents.jl StandardABM set up for generative agents.\n\nThe returned model is an ordinary StandardABM: it is stepped with Agents.step!, run with Agents.run!, and populated with Agents.add_agent! (or the convenience add_generative_agent!). generative_abm only guarantees the two things a generative model additionally needs — a backend and a clock — by injecting :llm and :clock into the model's properties.\n\nKeyword arguments\n\nllm::AbstractLLM: the backend shared by every agent; read with abmllm. Required.\nagent_step!: the per-agent step function. Defaults to generative_step!.\nmodel_step!: the per-step model function. Defaults to clock_step!, which advances the tick counter.\nspace: any Agents.jl space, or nothing (the default) for a non-spatial model.\nproperties: extra model properties, merged with :llm and :clock.\nagent_type: the agent type. Defaults to GenerativeAgent; a spatial model passes its own @agent type with a mind field.\nfurther kwargs are forwarded to StandardABM (e.g. scheduler, rng).\n\nExample\n\nmodel = generative_abm(; llm = ScriptedLLM(\"ACTION: wave\\nREASON: friendly\"),\n                          agent_step! = my_step!)\nadd_generative_agent!(model, Persona(\"Ada\"))\nstep!(model, 3)\n\n\n\n\n\n","category":"function"},{"location":"api/model/#GABM.add_generative_agent!","page":"Models and Simulation","title":"GABM.add_generative_agent!","text":"add_generative_agent!(model, persona; memory, plan, status, kwargs...) -> GenerativeAgent\n\nBuild a Mind around persona and add a GenerativeAgent carrying it to model, returning the new agent.\n\nmemory, plan, and status are forwarded to the Mind constructor; any further kwargs (e.g. a position) are forwarded to Agents.add_agent!. This is a convenience for the common case — a model with a custom agent type adds agents with Agents.add_agent! directly.\n\n\n\n\n\n","category":"function"},{"location":"api/model/#GABM.abmllm","page":"Models and Simulation","title":"GABM.abmllm","text":"abmllm(model) -> AbstractLLM\n\nReturn the language-model backend stored in model. Inside a step function, this is how an agent reaches the shared AbstractLLM.\n\n\n\n\n\n","category":"function"},{"location":"api/model/#GABM.abmclock","page":"Models and Simulation","title":"GABM.abmclock","text":"abmclock(model) -> Int\n\nReturn the current tick of model. The clock starts at 0 and is advanced by clock_step! once per step; pass it as the tick argument to cognitive functions.\n\n\n\n\n\n","category":"function"},{"location":"api/model/#GABM.clock_step!","page":"Models and Simulation","title":"GABM.clock_step!","text":"clock_step!(model)\n\nAdvance the model clock by one tick. This is the default model_step! used by generative_abm; a model that needs its own model_step! should call clock_step! from within it so that abmclock stays accurate.\n\n\n\n\n\n","category":"function"},{"location":"api/model/#GABM.generative_step!","page":"Models and Simulation","title":"GABM.generative_step!","text":"generative_step!(agent, model)\n\nThe default per-agent step: it calls maybe_reflect! so that each agent consolidates its memories into reflections once enough salient experience has accumulated.\n\nThis default deliberately does not make agents act, because acting is domain-specific — what an agent can do depends on the model. A real model supplies its own agent_step!, typically one that perceives the environment, calls decide, and applies the result. See the Building Models guide for the pattern.\n\n\n\n\n\n","category":"function"},{"location":"api/agents/#Personas-and-Agents","page":"Personas and Agents","title":"Personas and Agents","text":"A generative agent is built in two layers. The persona is its fixed identity — text that is legible to a language model. The mind wraps that persona around a memory stream, a plan, and a status, and is what every cognitive function reads and writes. The agent is the Agents.jl body that carries a mind through a simulation.\n\nThe split between mind and body is deliberate: the cognitive layer dispatches on the mind accessor, never on a concrete agent type, so the same machinery serves the ready-made non-spatial GenerativeAgent and any spatial @agent type with a mind field equally well.","category":"section"},{"location":"api/agents/#Personas","page":"Personas and Agents","title":"Personas","text":"A Persona is the part of an agent that does not change during a run. It is rendered into every cognitive prompt by describe.","category":"section"},{"location":"api/agents/#Minds","page":"Personas and Agents","title":"Minds","text":"A Mind is the cognitive state of an agent: a persona, a MemoryStream, a plan, and a status line.","category":"section"},{"location":"api/agents/#Agents","page":"Personas and Agents","title":"Agents","text":"GenerativeAgent is the ready-made body for non-spatial models. For a spatial model, define an agent with Agents.@agent that has a mind field; the mind and persona accessors then apply to it automatically.","category":"section"},{"location":"api/agents/#See-also","page":"Personas and Agents","title":"See also","text":"Memory and Retrieval — the MemoryStream inside every mind.\nCognition and LLMs — the operations that act on a mind.\nModels and Simulation — adding agents to a model.","category":"section"},{"location":"api/agents/#GABM.Persona","page":"Personas and Agents","title":"GABM.Persona","text":"Persona(name; age, occupation, traits, background)\n\nThe natural-language identity of a generative agent.\n\nA persona is the part of an agent that does not change during a simulation: a stable description of who the agent is, as opposed to the MemoryStream that records what has happened to it. It is rendered into every prompt the agent's mind issues, via describe.\n\nArguments\n\nname::AbstractString: the agent's name (positional, required).\nage: an Integer, or nothing if unspecified.\noccupation::AbstractString: a short role description.\ntraits: an iterable of short trait strings (e.g. [\"curious\", \"frugal\"]).\nbackground::AbstractString: one or more sentences of free-text history.\n\nExample\n\nisabella = Persona(\"Isabella Rodriguez\";\n    age = 34,\n    occupation = \"café owner\",\n    traits = [\"warm\", \"organised\", \"ambitious\"],\n    background = \"Runs Hobbs Cafe and is planning a Valentine's Day party.\")\n\n\n\n\n\n","category":"type"},{"location":"api/agents/#GABM.describe","page":"Personas and Agents","title":"GABM.describe","text":"describe(p::Persona) -> String\n\nRender a persona as a compact, prompt-ready identity block.\n\nOnly the fields that were supplied appear, each on its own line, so the result stays readable whether a persona was specified richly or minimally. This is the exact text that the cognitive layer (decide, reflect!, plan!) places at the head of every prompt.\n\nExample\n\njulia> println(describe(Persona(\"Ada\"; occupation = \"engineer\")))\nName: Ada\nOccupation: engineer\n\n\n\n\n\n","category":"function"},{"location":"api/agents/#GABM.Mind","page":"Personas and Agents","title":"GABM.Mind","text":"Mind(persona; memory, plan, status)\n\nThe cognitive state of a generative agent.\n\nA Mind bundles the four things every cognitive function in GABM.jl needs:\n\npersona::Persona — the agent's fixed identity (see Persona).\nmemory::MemoryStream — its evolving record of experience.\nplan::Vector{String} — its current intentions, as set by plan!.\nstatus::String — a one-line description of what it is doing right now.\n\nThe split between Mind and the agent body is deliberate. A Mind carries everything the language model reasons about; the body carries everything the spatial or network model needs. Keeping them in separate objects lets the same cognitive machinery serve a non-spatial GenerativeAgent, a grid agent, or a node in a social network without change.\n\n\n\n\n\n","category":"type"},{"location":"api/agents/#GABM.GenerativeAgent","page":"Personas and Agents","title":"GABM.GenerativeAgent","text":"@agent struct GenerativeAgent(NoSpaceAgent)\n    mind::Mind\nend\n\nA ready-made, non-spatial generative agent.\n\nGenerativeAgent is an Agents.jl agent with two fields: the id supplied by NoSpaceAgent, and a Mind. It is the agent type used by generative_abm unless another is requested, and it covers every model in which agents have no position — conversations, markets, committees, opinion dynamics.\n\nFor a spatial model, define an agent with a mind field instead, e.g.\n\nusing Agents, GABM\n@agent struct Resident(GridAgent{2})\n    mind::Mind\nend\n\nEvery cognitive function (observe!, decide, …) accepts such an agent directly, because they dispatch on the mind accessor rather than on the concrete agent type.\n\n\n\n\n\n","category":"type"},{"location":"api/agents/#GABM.mind","page":"Personas and Agents","title":"GABM.mind","text":"mind(agent) -> Mind\n\nReturn the Mind of agent.\n\nThe fallback implementation returns agent.mind, so any Agents.jl agent with a mind field works with GABM.jl out of the box. The cognitive layer is written entirely against this accessor; a modeller whose agent stores its mind elsewhere need only add a method to mind.\n\n\n\n\n\n","category":"function"},{"location":"api/agents/#GABM.persona","page":"Personas and Agents","title":"GABM.persona","text":"persona(x) -> Persona\n\nReturn the Persona of a Mind or of any agent that has one.\n\n\n\n\n\n","category":"function"},{"location":"guide/cognition/#The-Cognitive-Loop","page":"The Cognitive Loop","title":"The Cognitive Loop","text":"A generative agent does four things with its memory stream: it perceives, it abstracts, it intends, and it acts. GABM.jl implements each as one function — observe!, reflect!, plan!, decide — and this page describes them in the order Park et al. (2023) present them. A fifth function, converse, composes decision-style calls into a dialogue.\n\nEvery function on this page accepts either a Mind or an agent that has one: they dispatch on the mind accessor, so the same call works on a bare Mind during prototyping and on a GenerativeAgent inside a running model.","category":"section"},{"location":"guide/cognition/#Perceive-—-observe!","page":"The Cognitive Loop","title":"Perceive — observe!","text":"observe! writes a perception into the stream as an :observation:\n\nobserve!(agent, \"The market square is crowded and noisy.\"; tick = 12)\n\nThe only subtlety is importance. If importance is given, it is used. If it is omitted and a backend is supplied, GABM.jl calls rate_importance, which asks the language model to score the memory's poignancy on the Park et al. 1–10 scale:\n\nobserve!(agent, \"My closest friend is moving away.\"; tick = 12, llm = llm)\n\nLetting the model rate importance is what makes an agent's sense of what matters emergent rather than hand-coded — but it costs an API call per observation, so a model with cheap, plentiful perceptions often passes a fixed importance instead and reserves rating for events that might be pivotal.","category":"section"},{"location":"guide/cognition/#Abstract-—-reflect!","page":"The Cognitive Loop","title":"Abstract — reflect!","text":"Observations alone make a shallow agent: it can recall what happened but has drawn no conclusions from it. reflect! is the step that builds the higher layer. It runs in three stages, following Park et al. (2023):\n\nThe most recent memories are shown to the model, which is asked for the few most salient questions they raise.\nFor each question, retrieve gathers supporting evidence from anywhere in the stream — including earlier reflections.\nThe model is asked to infer one insight per question; each insight is written back as a :reflection, with citations pointing to its evidence.\n\nnew_insights = reflect!(agent, llm; tick = 50)\n\nBecause reflections are themselves memories, the layer compounds: after many runs of reflect! an agent holds reflections drawn from earlier reflections, a deepening self-model rather than a flat log.\n\nReflection is expensive — several model calls — so it should be periodic, not constant. maybe_reflect! is the trigger Park et al. describe: it sums the importance of every observation formed since the last reflection and only reflects when that total crosses a threshold.\n\nmaybe_reflect!(agent, llm; tick = abmclock(model), threshold = 100.0)\n\nA quiet agent reflects rarely; one living through eventful, high-importance ticks reflects often. This is exactly what the default generative_step! does, so agents in a model consolidate their memories without the modeller wiring it up.","category":"section"},{"location":"guide/cognition/#Intend-—-plan!","page":"The Cognitive Loop","title":"Intend — plan!","text":"plan! has the agent draw up a short plan. The prompt is built from the persona, the current status, an optional context string from the model, and — importantly — the reflections most relevant to the planning horizon, so the plan is grounded in what the agent has concluded about itself.\n\nplan!(agent, llm; tick = 0, horizon = \"the day ahead\")\n\nThe plan is stored on mind(agent).plan as a list of steps and also recorded as a :plan memory, so the agent can later recall what it had intended — and notice when events forced a departure from it.","category":"section"},{"location":"guide/cognition/#Act-—-decide","page":"The Cognitive Loop","title":"Act — decide","text":"decide is the step the rest of the simulation waits on. It retrieves the memories most relevant to a situation, assembles a prompt from the persona, the status, the current plan, and those memories, and returns a Decision:\n\nd = decide(agent, llm, \"A stranger asks to share your table.\"; tick = 30)\nd.action      # a short imperative phrase\nd.reasoning   # one sentence of justification\nd.raw         # the model's unparsed reply, kept for logging","category":"section"},{"location":"guide/cognition/#Constrained-choice","page":"The Cognitive Loop","title":"Constrained choice","text":"Left unconstrained, an agent invents its own action — open-ended behaviour, which is sometimes the point. But a classical agent-based model is built on a discrete choice set, and decide slots into one when given options:\n\nd = decide(agent, llm, \"The council votes on the new water tariff.\";\n           tick = 30, options = [\"vote for\", \"vote against\", \"abstain\"])\n\nThe agent is now constrained to return one of the options. This is the precise seam between the two modelling traditions: the choice set is the modeller's, fixed and analysable, while the selection is the language model's, grounded in the agent's remembered history. A model can move an agent along this spectrum question by question.","category":"section"},{"location":"guide/cognition/#Remembering-the-situation","page":"The Cognitive Loop","title":"Remembering the situation","text":"By default decide also writes the situation into the stream as an observation, so the agent will recall having faced it. Pass remember_situation = false for hypothetical or counterfactual queries that should not become part of the agent's history.","category":"section"},{"location":"guide/cognition/#Converse-—-converse","page":"The Cognitive Loop","title":"Converse — converse","text":"converse generates a turn-taking dialogue between two minds:\n\ndialogue = converse(isabella, tom, llm;\n                    topic = \"the Valentine's Day party\", turns = 3, tick = 40)\n\nEach utterance is produced from the speaker's persona, the memories most relevant to the conversation, and the transcript so far; the speakers alternate. The result is a vector of name => utterance pairs. Unless remember = false, a summary of the dialogue is written back into both minds, so a conversation becomes a shared memory that can later be retrieved, reflected on, and acted upon — the mechanism by which information spreads through a population of generative agents.","category":"section"},{"location":"guide/cognition/#The-loop,-assembled","page":"The Cognitive Loop","title":"The loop, assembled","text":"In a running model the four operations compose into a per-tick cycle inside agent_step!:\n\nfunction agent_step!(agent, model)\n    llm, t = abmllm(model), abmclock(model)\n    observe!(agent, perceive(agent, model); tick = t, llm = llm)   # perceive\n    maybe_reflect!(agent, llm; tick = t)                            # abstract\n    if t % 24 == 0\n        plan!(agent, llm; tick = t)                                 # intend\n    end\n    d = decide(agent, llm, situation(agent, model); tick = t)       # act\n    apply!(agent, model, d.action)\nend\n\nperceive, situation, and apply! are the modeller's — they are where the cognitive loop meets the specific world being simulated. The Building Models guide develops them for a concrete model.","category":"section"},{"location":"guide/cognition/#Where-to-next","page":"The Cognitive Loop","title":"Where to next","text":"Building Models — wiring this loop into an Agents.jl model.\nCognition and LLMs — the API reference for every function on this page.","category":"section"},{"location":"guide/llm/#Language-Models","page":"Language Models","title":"Language Models","text":"Every cognitive operation in GABM.jl — rating a memory, synthesising a reflection, choosing an action — ends in a call to a language model. This page describes the interface those calls go through and the two backends GABM.jl ships.","category":"section"},{"location":"guide/llm/#The-AbstractLLM-interface","page":"Language Models","title":"The AbstractLLM interface","text":"GABM.jl never calls a provider's SDK directly. All model access goes through AbstractLLM, an abstract type with a deliberately small contract. A backend is anything that implements one method:\n\ncomplete(llm, prompt; system = \"\", temperature) -> String\n\ncomplete takes a prompt and returns a completion. The optional system argument supplies a system prompt; temperature controls sampling randomness. A backend may additionally implement\n\nembed(llm, text) -> Vector{Float64}\n\nand signal that it has done so by defining supports_embeddings(llm) = true. Embeddings are used only to score the relevance of memories during retrieve; a backend without them still runs a complete model, because retrieval falls back to lexical overlap.\n\nThis narrow interface is the reason a generative model is portable. The simulation is written against AbstractLLM, so the same model definition runs against a deterministic mock during development and a live provider in production, with no change to a single cognitive call.","category":"section"},{"location":"guide/llm/#The-scripted-backend","page":"Language Models","title":"The scripted backend","text":"ScriptedLLM is a deterministic mock that never touches the network. It is the backend to develop and test against: it is free, instant, and exactly reproducible. It has three forms.\n\nA queue returns a fixed list of replies, one per call, and raises LLMError when exhausted — so a missing reply fails loudly:\n\nllm = ScriptedLLM([\"ACTION: wave\", \"ACTION: smile\"])\ncomplete(llm, \"...\")   # \"ACTION: wave\"\ncomplete(llm, \"...\")   # \"ACTION: smile\"\n\nA responder computes each reply from the prompt. This is the most useful form for a model with many cognitive calls, because one function can answer every kind of prompt the simulation issues:\n\nllm = ScriptedLLM() do prompt\n    occursin(\"Rating:\", prompt) ? \"7\" :\n    occursin(\"ACTION:\", prompt) ? \"ACTION: cooperate\\nREASON: trust pays\" :\n    \"I see.\"\nend\n\nA constant returns the same string for every call — useful for the simplest smoke tests:\n\nllm = ScriptedLLM(\"yes\")\n\nEvery prompt a ScriptedLLM receives is appended to its log field, so a test can assert on exactly what the cognitive layer asked:\n\nllm.log    # Vector{String} of all prompts seen\n\nGABM.reset! rewinds a queue-backed ScriptedLLM and clears its log, so a deterministic scenario can be re-run.","category":"section"},{"location":"guide/llm/#The-live-backend","page":"Language Models","title":"The live backend","text":"PromptingToolsLLM is the production backend. It forwards to PromptingTools.jl, which is itself provider-agnostic: a single PromptingToolsLLM can drive Anthropic, OpenAI, Mistral, Groq, or a model served locally by Ollama. The provider is selected entirely by the model string.\n\nllm = PromptingToolsLLM(model = \"claudeh\", temperature = 0.7)\n\nThe keyword arguments are:\n\nArgument Default Meaning\nmodel \"claudeh\" Chat model, as a PromptingTools.jl name or alias\nembedding_model \"text-embedding-3-small\" Model used by embed\ntemperature 0.7 Default sampling temperature\nmax_tokens 1024 Maximum tokens per completion\n\nAPI keys are read from the environment by PromptingTools.jl, not by GABM.jl — set ENV[\"ANTHROPIC_API_KEY\"], ENV[\"OPENAI_API_KEY\"], and so on, or register custom models, following the PromptingTools.jl documentation. When a provider call fails, the error is wrapped in an LLMError that names the model involved.","category":"section"},{"location":"guide/llm/#Choosing-a-backend","page":"Language Models","title":"Choosing a backend","text":"A typical project uses both. The model is developed and unit-tested against a ScriptedLLM — fast iteration, no cost, reproducible failures — and then run for real by substituting a PromptingToolsLLM:\n\nllm = run_for_real ? PromptingToolsLLM(model = \"claudeh\") :\n                     ScriptedLLM(my_responder)\n\nmodel = generative_abm(; llm = llm, agent_step! = agent_step!)\n\nBecause the backend is just one object passed to generative_abm, nothing else in the model is aware of which one is in use.","category":"section"},{"location":"guide/llm/#Temperature-and-reproducibility","page":"Language Models","title":"Temperature and reproducibility","text":"A classical Agents.jl model is made reproducible by seeding its random number generator. A generative model has a second source of randomness: the language model's own sampling, governed by temperature. Two settings bracket the useful range.\n\ntemperature = 0.0 makes a provider as close to deterministic as it can be, and is the right choice for importance ratings and for runs intended to be comparable.\nA higher temperature (0.7–1.0) produces the behavioural variety that makes a population of generative agents heterogeneous even when their personas are similar.\n\nFor exact reproducibility — a regression test, a figure in a paper — use a ScriptedLLM: it is the only backend whose output does not depend on a remote service at all.","category":"section"},{"location":"guide/llm/#Implementing-a-custom-backend","page":"Language Models","title":"Implementing a custom backend","text":"Any new backend is a subtype of AbstractLLM with a complete method. A backend that wraps an in-house model, adds caching, or logs token usage need implement nothing more:\n\nstruct CachingLLM <: AbstractLLM\n    inner::AbstractLLM\n    cache::Dict{String,String}\nend\n\nfunction GABM.complete(llm::CachingLLM, prompt::AbstractString; kwargs...)\n    get!(llm.cache, prompt) do\n        complete(llm.inner, prompt; kwargs...)\n    end\nend\n\nSuch a backend drops straight into generative_abm; the cognitive layer cannot tell the difference.","category":"section"},{"location":"guide/llm/#Where-to-next","page":"Language Models","title":"Where to next","text":"The Memory Stream — what the cognitive layer reads and writes.\nThe Cognitive Loop — the operations that issue these calls.\nCognition and LLMs — the API reference for the backend types.","category":"section"},{"location":"api/memory/#Memory-and-Retrieval","page":"Memory and Retrieval","title":"Memory and Retrieval","text":"The memory stream is the central data structure of a generative agent: an append-only, timestamped log of natural-language records. This page documents the types that make it up and the retrieve function that selects from it. The Memory Stream guide explains the retrieval scoring in narrative form and works it through an example.","category":"section"},{"location":"api/memory/#Memory-entries","page":"Memory and Retrieval","title":"Memory entries","text":"A MemoryEntry is one record in a stream — an observation, a reflection, or a plan, as enumerated by MEMORY_KINDS.","category":"section"},{"location":"api/memory/#The-memory-stream","page":"Memory and Retrieval","title":"The memory stream","text":"A MemoryStream holds an agent's entries together with the parameters — a recency decay and three retrieval weights — that govern how they are recalled.","category":"section"},{"location":"api/memory/#Retrieval","page":"Memory and Retrieval","title":"Retrieval","text":"retrieve is the function of Park et al. (2023): it scores every candidate memory on recency, importance, and relevance, and returns the highest-scoring few. embed_memories! populates the embeddings that let the relevance term use cosine similarity rather than the lexical fallback.","category":"section"},{"location":"api/memory/#See-also","page":"Memory and Retrieval","title":"See also","text":"The Memory Stream — the retrieval formula in detail.\nCognition and LLMs — observe!, reflect!, and plan! all write to the stream.","category":"section"},{"location":"api/memory/#GABM.MemoryEntry","page":"Memory and Retrieval","title":"GABM.MemoryEntry","text":"MemoryEntry(content; kind, importance, created, last_accessed, embedding, citations)\n\nA single record in a MemoryStream.\n\nFields\n\ncontent::String: the memory itself, in natural language.\nkind::Symbol: one of MEMORY_KINDS.\nimportance::Float64: poignancy on the Park et al. 1–10 scale, where 1 is mundane and 10 is profound. See rate_importance.\ncreated::Int: the simulation tick at which the memory was formed.\nlast_accessed::Int: the tick at which the memory was most recently returned by retrieve; recency decay is measured from this value, so retrieving a memory rejuvenates it.\nembedding::Union{Nothing,Vector{Float64}}: an optional dense vector used for relevance scoring.\ncitations::Vector{Int}: indices of the memories a reflection was derived from; empty for observations and plans.\n\n\n\n\n\n","category":"type"},{"location":"api/memory/#GABM.MEMORY_KINDS","page":"Memory and Retrieval","title":"GABM.MEMORY_KINDS","text":"MEMORY_KINDS\n\nThe recognised values of the kind field of a MemoryEntry:\n\n:observation — something the agent perceived or did.\n:reflection — a higher-level inference synthesised by reflect!.\n:plan — an intention recorded by plan!.\n\nThe distinction is the one Park et al. (2023) draw between the raw perceptual stream and the abstractions an agent builds on top of it.\n\n\n\n\n\n","category":"constant"},{"location":"api/memory/#GABM.MemoryStream","page":"Memory and Retrieval","title":"GABM.MemoryStream","text":"MemoryStream(; decay = 0.995, weights = (1.0, 1.0, 1.0))\n\nAn append-only, time-ordered log of an agent's MemoryEntry records.\n\nKeyword arguments\n\ndecay::Float64: the per-tick base of the exponential recency score. With the default 0.995, a memory not accessed for 100 ticks keeps about 60% of its recency weight; a smaller value forgets faster.\nweights::NTuple{3,Float64}: the (recency, importance, relevance) coefficients combined by retrieve. Raising one coordinate makes retrieval lean more heavily on that signal.\n\nA fresh stream is empty; add to it with remember! and query it with retrieve.\n\n\n\n\n\n","category":"type"},{"location":"api/memory/#GABM.remember!","page":"Memory and Retrieval","title":"GABM.remember!","text":"remember!(stream, content; kind, importance, tick, embedding, citations) -> Int\n\nAppend a memory to stream and return its index.\n\ncontent is the natural-language record; the keyword arguments map onto the fields of MemoryEntry. tick is the current simulation time and is recorded as both the creation and the initial last-accessed time.\n\nMost models do not call remember! directly: observe!, reflect!, and plan! call it with the appropriate kind.\n\n\n\n\n\n","category":"function"},{"location":"api/memory/#GABM.retrieve","page":"Memory and Retrieval","title":"GABM.retrieve","text":"retrieve(stream, query; tick, n = 5, kinds = nothing, llm = nothing,\n         weights = stream.weights) -> Vector{MemoryEntry}\n\nReturn the n memories from stream most worth surfacing for query.\n\nThis is the retrieval function of Park et al. (2023). Each candidate memory is scored on three components, each min-max normalised across the candidate set so the components are comparable:\n\nRecency — decay ^ (tick - last_accessed), an exponential decay that favours memories formed or used recently (see MemoryStream).\nImportance — the memory's stored poignancy, on the 1–10 scale.\nRelevance — similarity to query: cosine similarity of embeddings when both the query and the memory have them, and Jaccard token overlap otherwise.\n\nThe three normalised components are combined by weights and the top n entries are returned. Retrieving a memory updates its last_accessed tick to tick, so attention is self-reinforcing: a memory used now is easier to retrieve next time.\n\nKeyword arguments\n\ntick: the current simulation time (required).\nn: how many memories to return.\nkinds: if given, an iterable of MEMORY_KINDS to restrict the search to (e.g. kinds = (:reflection,)).\nllm: a backend used to embed the query when embedding-based relevance is wanted; pass the same backend that produced the memory embeddings.\nweights: override the stream's (recency, importance, relevance) weights for this query only.\n\n\n\n\n\n","category":"function"},{"location":"api/memory/#GABM.embed_memories!","page":"Memory and Retrieval","title":"GABM.embed_memories!","text":"embed_memories!(stream, llm) -> MemoryStream\n\nCompute and cache an embedding for every entry in stream that lacks one, using embed on the backend llm.\n\nCalling this once after a batch of observations lets subsequent retrieve calls score relevance by cosine similarity instead of the lexical fallback. It is a no-op — and raises nothing — if llm does not support embeddings.\n\n\n\n\n\n","category":"function"},{"location":"guide/memory/#The-Memory-Stream","page":"The Memory Stream","title":"The Memory Stream","text":"The memory stream is the one indispensable data structure of a generative agent. Everything an agent knows, has concluded, or intends is a timestamped record in it; every cognitive operation is a read from or a write to it. This page describes how the stream is built and, in detail, how retrieve selects from it.","category":"section"},{"location":"guide/memory/#What-a-memory-is","page":"The Memory Stream","title":"What a memory is","text":"A MemoryEntry is a natural-language record with a little structure around it:\n\nField Meaning\ncontent The memory itself, as a sentence of text\nkind :observation, :reflection, or :plan (see MEMORY_KINDS)\nimportance Poignancy on the 1–10 scale\ncreated The tick at which the memory was formed\nlast_accessed The tick at which it was most recently retrieved\nembedding An optional dense vector for relevance scoring\ncitations For a reflection, the memories it was inferred from\n\nThe kind field draws the distinction Park et al. (2023) make between the raw perceptual stream and the abstractions built on it. An :observation is something that happened; a :reflection is a conclusion the agent reached by reflect!; a :plan is an intention recorded by plan!. Because all three live in the same stream, a reflection can be retrieved as evidence for a later reflection, and a plan can be recalled when deciding.","category":"section"},{"location":"guide/memory/#Building-a-stream","page":"The Memory Stream","title":"Building a stream","text":"A MemoryStream starts empty. The low-level way to add to it is remember!:\n\nstream = MemoryStream()\nremember!(stream, \"I opened the café at seven.\"; kind = :observation,\n          importance = 3.0, tick = 1)\n\nIn practice a model rarely calls remember! directly. The cognitive operations — observe!, reflect!, plan! — call it with the right kind and a sensible importance, and that is the interface the Cognitive Loop guide describes.\n\nThe stream is configured by two parameters set at construction:\n\nstream = MemoryStream(decay = 0.99, weights = (1.0, 1.0, 1.0))\n\ndecay is the per-tick base of the recency score, and weights are the three coefficients combined during retrieval. Both are explained below.","category":"section"},{"location":"guide/memory/#The-retrieval-problem","page":"The Memory Stream","title":"The retrieval problem","text":"After a few hundred ticks an agent's stream holds far more than can be placed in a single prompt. Retrieval is the act of choosing, for a given query, the handful of memories worth surfacing. GABM.jl scores each candidate memory on the three signals of Park et al. (2023) — recency, importance, relevance — normalises them, combines them with the stream's weights, and returns the top-scoring few.\n\nFor a query q at tick t, memory m scores\n\ntextscore(m mid qt) =\n   w_textrecwidetilde r(mt) +\n   w_textimpwidetilde p(m) +\n   w_textrelwidetilde s(mq)","category":"section"},{"location":"guide/memory/#Recency","page":"The Memory Stream","title":"Recency","text":"The raw recency of a memory decays exponentially with the time since it was last touched:\n\nr(mt) = delta^t - a(m)\n\nwhere a(m) is the last_accessed tick and delta is the stream's decay. With the default delta = 0995, a memory unused for 100 ticks keeps 0995^100 approx 061 of its recency weight; with delta = 09 it keeps only 09^100 approx 27times10^-5 and is effectively forgotten. Lowering decay gives the agent a shorter memory.\n\nCrucially, retrieving a memory sets a(m) to the current tick. Recall is therefore self-reinforcing: a memory used today is fresh again tomorrow, while one never recalled fades, exactly as Park et al. intended.","category":"section"},{"location":"guide/memory/#Importance","page":"The Memory Stream","title":"Importance","text":"The raw importance is the memory's stored poignancy, p(m) in 110, rescaled to 01. It is fixed when the memory is formed — either passed explicitly or, when a backend is available, rated by rate_importance. Importance is what stops a consequential but old memory (a betrayal, a windfall) from being buried under a heap of trivial recent ones.","category":"section"},{"location":"guide/memory/#Relevance","page":"The Memory Stream","title":"Relevance","text":"The raw relevance s(mq) measures how much memory m bears on query q. GABM.jl computes it two ways:\n\nEmbeddings. If the query can be embedded and the memory has a cached embedding, relevance is their cosine similarity, mapped to 01. Call embed_memories! once to populate the embeddings, and pass the backend to retrieve.\nLexical fallback. Otherwise, relevance is the Jaccard overlap of the content-word sets of the query and the memory. No backend, no embedding endpoint, and no extra API calls are needed — the model still runs.\n\nThe fallback is coarse but keeps a model fully functional offline, which is why the examples in this manual run end-to-end against a ScriptedLLM.","category":"section"},{"location":"guide/memory/#Normalisation-and-weighting","page":"The Memory Stream","title":"Normalisation and weighting","text":"The three raw scores live on different scales, so each is min–max normalised across the candidate set before being combined:\n\nwidetilde x_i = fracx_i - min_j x_jmax_j x_j - min_j x_j\n\nA component on which every candidate is equal — say, importance when all memories were rated 5 — collapses to all-zeros and silently drops out of the sum, so it never distorts the ranking. The normalised components are then combined with weights, and the top n memories are returned.\n\nTilting the weights changes the character of recall. weights = (0, 0, 1) gives pure semantic search; weights = (2, 1, 1) produces a present-focused agent that mostly recalls what just happened; the default (1, 1, 1) balances all three.","category":"section"},{"location":"guide/memory/#A-worked-retrieval","page":"The Memory Stream","title":"A worked retrieval","text":"Put four memories in a stream and query it:\n\nstream = MemoryStream()\nremember!(stream, \"I argued with my brother about money.\"; tick = 2,  importance = 9.0)\nremember!(stream, \"I bought milk and bread.\";              tick = 40, importance = 2.0)\nremember!(stream, \"My brother apologised for the argument.\"; tick = 41, importance = 8.0)\nremember!(stream, \"It rained in the afternoon.\";           tick = 42, importance = 1.0)\n\ntop = retrieve(stream, \"my relationship with my brother\"; tick = 45, n = 2)\n\nWith the default weights the two brother memories win. Each is recent enough, the argument scores highest on importance, and both overlap the query on the words brother and (for the second) argument. The grocery and weather memories — recent but trivial and irrelevant — are left behind. Retrieving the two brother memories also advances their last_accessed to tick 45, so the next query will find them fresher still.","category":"section"},{"location":"guide/memory/#Reflections-cite-their-evidence","page":"The Memory Stream","title":"Reflections cite their evidence","text":"When reflect! writes a reflection, it records in the entry's citations field the indices of the memories the inference was drawn from. This makes a reflection traceable: from any conclusion an agent has reached, a modeller can follow the citations back to the observations that produced it. For long runs this is the main tool for auditing why an agent behaves as it does.","category":"section"},{"location":"guide/memory/#Where-to-next","page":"The Memory Stream","title":"Where to next","text":"The Cognitive Loop — the operations that read and write the stream.\nMemory and Retrieval — the full API reference for the types and functions on this page.","category":"section"},{"location":"guide/models/#Building-Models","page":"Building Models","title":"Building Models","text":"The previous guides covered the parts in isolation — a backend, a memory stream, the cognitive loop. This page assembles them into a complete, runnable agent-based model and then shows how to take it spatial.","category":"section"},{"location":"guide/models/#A-generative-model-is-an-Agents.jl-model","page":"Building Models","title":"A generative model is an Agents.jl model","text":"GABM.jl adds no simulation engine of its own. A generative model is an Agents.jl StandardABM; generative_abm only guarantees it carries the two things the cognitive layer needs:\n\na shared AbstractLLM backend, reachable with abmllm; and\nan integer clock starting at 0, read with abmclock and advanced once per step by clock_step!.\n\nEverything else — stepping, running, data collection, the random number generator — is Agents.jl, unchanged and fully available.","category":"section"},{"location":"guide/models/#A-worked-model:-deciding-to-adopt","page":"Building Models","title":"A worked model: deciding to adopt","text":"We build a small model of social adoption. A handful of villagers each decide, tick by tick, whether to adopt a new irrigation method; they talk to one another, and what they hear shapes what they later decide. The whole model runs offline against a ScriptedLLM.","category":"section"},{"location":"guide/models/#The-agents","page":"Building Models","title":"The agents","text":"The villagers have no position — they form a single conversational community — so the ready-made non-spatial GenerativeAgent is the right body. Each gets a Persona:\n\nusing GABM\n\npersonas = [\n    Persona(\"Wbishere\"; occupation = \"smallholder\", traits = [\"cautious\", \"respected\"]),\n    Persona(\"Adsila\";   occupation = \"smallholder\", traits = [\"curious\", \"early adopter\"]),\n    Persona(\"Catori\";   occupation = \"smallholder\", traits = [\"skeptical\", \"frugal\"]),\n]","category":"section"},{"location":"guide/models/#The-backend","page":"Building Models","title":"The backend","text":"For development we use a prompt-aware ScriptedLLM. One responder function answers every kind of cognitive prompt the model will issue:\n\nfunction responder(prompt)\n    if occursin(\"Rating:\", prompt)\n        return \"6\"\n    elseif occursin(\"ACTION:\", prompt)\n        return occursin(\"early adopter\", prompt) ?\n            \"ACTION: adopt the new method\\nREASON: the trial yields look strong\" :\n            \"ACTION: wait and watch the neighbours\\nREASON: the method is unproven\"\n    else\n        return \"I have been thinking about the new irrigation method.\"\n    end\nend\n\nllm = ScriptedLLM(responder)","category":"section"},{"location":"guide/models/#The-step-function","page":"Building Models","title":"The step function","text":"The cognitive loop lives in agent_step!. Each tick a villager observes the current state of opinion around it, then decides between two explicit options — the discrete choice set that keeps the model analysable:\n\nfunction agent_step!(agent, model)\n    t   = abmclock(model)\n    llm = abmllm(model)\n\n    adopters = count(a -> mind(a).status == \"adopter\", allagents(model))\n    observe!(agent, \"At time $t, $adopters villagers have adopted the method.\";\n             tick = t, importance = 4.0)\n\n    d = decide(agent, llm,\n               \"Villager $(persona(agent).name) must decide about the method.\";\n               tick = t, options = [\"adopt the new method\",\n                                     \"wait and watch the neighbours\"])\n\n    if occursin(\"adopt\", d.action)\n        mind(agent).status = \"adopter\"\n    end\n    return nothing\nend\n\nThe model step is left as the default clock_step!, which advances the tick counter.","category":"section"},{"location":"guide/models/#Assembling-and-populating","page":"Building Models","title":"Assembling and populating","text":"model = generative_abm(; llm = llm, agent_step! = agent_step!)\n\nfor p in personas\n    add_generative_agent!(model, p; status = \"undecided\")\nend\n\nadd_generative_agent! builds a Mind around each persona and adds a GenerativeAgent to the model.","category":"section"},{"location":"guide/models/#Running-and-collecting-data","page":"Building Models","title":"Running and collecting data","text":"The model is an ordinary Agents.jl model, so run! drives it and collects results into a DataFrame. We record each agent's status every tick:\n\nstatus(agent) = mind(agent).status\n\nadf, mdf = run!(model, 10; adata = [status])\nadf      # one row per agent per step — the adoption curve\n\nadf is a standard DataFrame; group it, plot it, or compare runs exactly as with any Agents.jl model.","category":"section"},{"location":"guide/models/#Adding-conversation","page":"Building Models","title":"Adding conversation","text":"Adoption spreads through talk. To let it, have agents converse before they decide — a conversation writes a shared memory into both participants, and decide will retrieve it:\n\nfunction agent_step!(agent, model)\n    t, llm = abmclock(model), abmllm(model)\n    others = filter(a -> a !== agent, collect(allagents(model)))\n    if !isempty(others)\n        partner = rand(abmrng(model), others)\n        converse(agent, partner, llm; topic = \"the new irrigation method\",\n                 turns = 2, tick = t)\n    end\n    d = decide(agent, llm, \"Decide about the method.\"; tick = t,\n               options = [\"adopt the new method\", \"wait and watch the neighbours\"])\n    occursin(\"adopt\", d.action) && (mind(agent).status = \"adopter\")\n    return nothing\nend\n\nUsing abmrng(model) rather than a bare rand keeps the run reproducible: seed the model's generator and the sequence of conversation partners is fixed, even though the language model's replies may not be.","category":"section"},{"location":"guide/models/#Reflection-between-ticks","page":"Building Models","title":"Reflection between ticks","text":"The default generative_step! calls maybe_reflect!, so agents consolidate memories on their own. A custom agent_step! replaces that default, so add the call back if you want it:\n\nfunction agent_step!(agent, model)\n    t, llm = abmclock(model), abmllm(model)\n    # ... perceive, converse, decide ...\n    maybe_reflect!(agent, llm; tick = t, threshold = 80.0)\n    return nothing\nend","category":"section"},{"location":"guide/models/#Spatial-models","page":"Building Models","title":"Spatial models","text":"When agents do have a position, the only change is the agent type. The GenerativeAgent is non-spatial; for a spatial model define your own Agents.jl agent with Agents.@agent and give it a mind field:\n\nusing Agents, GABM\n\n@agent struct Resident(GridAgent{2})\n    mind::Mind\nend\n\nThe cognitive functions need no change: they dispatch on the mind accessor, and the fallback mind(agent) = agent.mind already covers any agent with that field. Pass the type and a space to generative_abm:\n\nmodel = generative_abm(; llm = llm,\n                          agent_type = Resident,\n                          space = GridSpace((20, 20)),\n                          agent_step! = agent_step!)\n\nadd_agent!(model, Mind(Persona(\"Resident 1\")))   # standard Agents.jl call\n\nInside agent_step!, the spatial neighbourhood becomes the agent's perception — nearby_agents, nearby_positions, and the rest of the Agents.jl spatial API are what perceive and situation are built from. The same pattern applies to GraphSpace for network models and ContinuousSpace for off-lattice ones.","category":"section"},{"location":"guide/models/#The-shape-of-every-model","page":"Building Models","title":"The shape of every model","text":"However elaborate the world, a generative model has the same four moving parts:\n\n  generative_abm(...)   ── the StandardABM, carrying llm + clock\n        │\n        ├── agent_type   ── GenerativeAgent, or an @agent with a `mind`\n        │\n        ├── agent_step!  ── perceive → (reflect) → (plan) → decide → apply\n        │\n        └── run!(model, n; adata, mdata)   ── ordinary Agents.jl analysis\n\nMaster those and any GABM.jl model is a variation on them.","category":"section"},{"location":"guide/models/#Where-to-next","page":"Building Models","title":"Where to next","text":"Models and Simulation — the API reference for generative_abm and the model helpers.\nThe Cognitive Loop — the operations called inside agent_step!.\nReferences — the modelling literature behind the design.","category":"section"},{"location":"getting_started/#Getting-Started","page":"Getting Started","title":"Getting Started","text":"This page installs GABM.jl and builds a complete generative agent-based model. The whole walkthrough runs offline: it uses the deterministic ScriptedLLM backend, so it needs no API key, costs nothing, and produces the same result every time. Swapping in a live model is a one-line change, shown at the end.","category":"section"},{"location":"getting_started/#Installation","page":"Getting Started","title":"Installation","text":"GABM.jl requires Julia 1.10 or newer. From the Julia REPL:\n\nusing Pkg\nPkg.add(url = \"https://github.com/simoneSantoni/GABM.jl\")\n\nThe package pulls in Agents.jl for the simulation engine and PromptingTools.jl for live language-model access. Load it with:\n\nusing GABM","category":"section"},{"location":"getting_started/#A-first-mind","page":"Getting Started","title":"A first mind","text":"The smallest unit of GABM.jl is a Mind: a Persona — the agent's fixed identity — wrapped around a MemoryStream that will fill up as the simulation runs.\n\nisabella = Mind(Persona(\"Isabella Rodriguez\";\n    age = 34,\n    occupation = \"café owner\",\n    traits = [\"warm\", \"organised\", \"ambitious\"],\n    background = \"Runs Hobbs Cafe and is thinking about hosting an event.\"))\n\nNothing has happened to Isabella yet, so her memory stream is empty. We give her an experience with observe!, passing the current tick:\n\nobserve!(isabella, \"The café was busy and three regulars asked for a place \" *\n                   \"to celebrate Valentine's Day.\"; tick = 1, importance = 7.0)\n\nThe importance argument is the memory's poignancy on the Park et al. 1–10 scale. Omit it and — if a language-model backend is supplied — GABM.jl will ask the model to rate it; see rate_importance.","category":"section"},{"location":"getting_started/#A-scripted-backend","page":"Getting Started","title":"A scripted backend","text":"Cognition needs a backend. For development we use ScriptedLLM in its responder form: a function from prompt to reply. It inspects each prompt and returns a plausible answer, so the whole model is deterministic.\n\nfunction backend(prompt)\n    if occursin(\"ACTION:\", prompt)\n        return \"ACTION: host a Valentine's Day party at the café\\n\" *\n               \"REASON: regulars asked for somewhere to celebrate\"\n    elseif occursin(\"brief, concrete plan\", prompt)\n        return \"Decorate the café\\nInvite the regulars\\nPrepare a set menu\"\n    else\n        return \"It sounds like a wonderful idea.\"\n    end\nend\n\nllm = ScriptedLLM(backend)\n\nA real run would instead use PromptingToolsLLM(); everything below is identical either way.","category":"section"},{"location":"getting_started/#Asking-the-agent-to-decide","page":"Getting Started","title":"Asking the agent to decide","text":"decide is the act step of the cognitive loop. It retrieves the memories most relevant to a situation, builds a prompt, and returns a Decision.\n\nd = decide(isabella, llm,\n    \"It is the first of February. Isabella is considering how to use the café.\"\n    ; tick = 5)\n\nd.action     # \"host a Valentine's Day party at the café\"\nd.reasoning  # \"regulars asked for somewhere to celebrate\"\n\nThe decision is grounded in the memory we planted: Isabella acts on the remembered request from her regulars, not on a rule we wrote down.","category":"section"},{"location":"getting_started/#Assembling-a-model","page":"Getting Started","title":"Assembling a model","text":"A single mind is useful for prototyping, but a simulation needs a population, a clock, and a step function. generative_abm builds an Agents.jl StandardABM carrying a shared backend and a tick counter.\n\nfunction agent_step!(agent, model)\n    tick = abmclock(model)\n    d = decide(agent, abmllm(model),\n               \"Day $tick at the café — what should $(persona(agent).name) do?\";\n               tick = tick)\n    mind(agent).status = d.action\n    observe!(agent, \"On day $tick I decided to $(d.action).\"; tick = tick)\nend\n\nmodel = generative_abm(; llm = llm, agent_step! = agent_step!)\n\nWe populate it with add_generative_agent!, which builds a GenerativeAgent — the ready-made non-spatial agent type — around a persona:\n\nadd_generative_agent!(model, Persona(\"Isabella Rodriguez\";\n    occupation = \"café owner\", traits = [\"warm\", \"ambitious\"]))\nadd_generative_agent!(model, Persona(\"Tom Moreno\";\n    occupation = \"pharmacist\", traits = [\"sociable\", \"busy\"]))","category":"section"},{"location":"getting_started/#Running-it","page":"Getting Started","title":"Running it","text":"The model is an ordinary Agents.jl model, so it steps and runs with the Agents.jl API (re-exported by GABM.jl for convenience):\n\nstep!(model, 3)        # advance three ticks\nabmclock(model)        # 3\n\nTo collect data, use Agents.jl's run! with an agent-data specification — here we record each agent's status every tick:\n\nstatus(agent) = mind(agent).status\nadf, _ = run!(model, 3; adata = [status])\nadf        # a DataFrame: one row per agent per step\n\nadf is a normal DataFrame; everything Agents.jl offers for analysis, plotting, and reproducibility applies unchanged.","category":"section"},{"location":"getting_started/#Going-live","page":"Getting Started","title":"Going live","text":"To run the same model against a real language model, change one line:\n\nllm = PromptingToolsLLM(model = \"claudeh\")\n\nPromptingToolsLLM forwards to PromptingTools.jl, which reads provider API keys from your environment (for Anthropic, set ENV[\"ANTHROPIC_API_KEY\"]; see the PromptingTools.jl documentation for other providers). The model definition, the step function, and the analysis code do not change at all.","category":"section"},{"location":"getting_started/#Where-to-next","page":"Getting Started","title":"Where to next","text":"Language Models — the backends in depth, and how to choose and configure them.\nThe Memory Stream — how memories are stored, scored, and retrieved.\nThe Cognitive Loop — observe!, reflect!, plan!, and decide.\nBuilding Models — spatial models, schedulers, and larger simulations.","category":"section"},{"location":"api/cognition/#Cognition-and-LLMs","page":"Cognition and LLMs","title":"Cognition and LLMs","text":"This page documents the two halves of generative cognition: the AbstractLLM backends that supply the reasoning, and the cognitive operations that use them to perceive, abstract, intend, and act. The Language Models and Cognitive Loop guides cover both in narrative form.","category":"section"},{"location":"api/cognition/#The-backend-interface","page":"Cognition and LLMs","title":"The backend interface","text":"Every cognitive call reaches an AbstractLLM. A backend implements complete and, optionally, embed.","category":"section"},{"location":"api/cognition/#Backends","page":"Cognition and LLMs","title":"Backends","text":"GABM.jl ships a live backend and a deterministic mock. The mock is the one to develop and test against; the live backend is a one-line substitution for a real run.","category":"section"},{"location":"api/cognition/#Importance-rating","page":"Cognition and LLMs","title":"Importance rating","text":"","category":"section"},{"location":"api/cognition/#The-cognitive-loop","page":"Cognition and LLMs","title":"The cognitive loop","text":"The four operations of generative cognition. Each reads or writes the agent's memory stream; each accepts either a Mind or an agent that has one.","category":"section"},{"location":"api/cognition/#Conversation","page":"Cognition and LLMs","title":"Conversation","text":"","category":"section"},{"location":"api/cognition/#See-also","page":"Cognition and LLMs","title":"See also","text":"Language Models — choosing and configuring a backend.\nThe Cognitive Loop — the operations in narrative form.\nModels and Simulation — calling these operations inside a step function.","category":"section"},{"location":"api/cognition/#GABM.AbstractLLM","page":"Cognition and LLMs","title":"GABM.AbstractLLM","text":"AbstractLLM\n\nSupertype for language-model backends.\n\nA backend is anything that can turn a prompt into a string. Concrete subtypes must implement complete; they may additionally implement embed and set supports_embeddings to true.\n\nGABM.jl ships two backends:\n\nPromptingToolsLLM — a live backend that forwards to PromptingTools.jl, and therefore to OpenAI, Anthropic, Ollama, or any other provider that package supports.\nScriptedLLM — a deterministic mock used in tests, documentation, and reproducible experiments, where calling a paid API is undesirable.\n\nKeeping the simulation code written against AbstractLLM means the same model can be run against a live provider or a scripted mock without changing a line of the model definition.\n\n\n\n\n\n","category":"type"},{"location":"api/cognition/#GABM.complete","page":"Cognition and LLMs","title":"GABM.complete","text":"complete(llm, prompt; system=\"\", temperature) -> String\n\nSend prompt to the backend llm and return its completion as a String.\n\nsystem is an optional system prompt that frames the request without being part of the conversation proper. temperature controls sampling randomness; its default is backend-specific.\n\nThis is the single primitive that every cognitive function in GABM.jl is built on. Any subtype of AbstractLLM must implement it.\n\n\n\n\n\n","category":"function"},{"location":"api/cognition/#GABM.embed","page":"Cognition and LLMs","title":"GABM.embed","text":"embed(llm, text) -> Vector{Float64}\n\nReturn a dense vector embedding of text.\n\nEmbeddings are optional: a backend that provides them must also set supports_embeddings to true. When embeddings are available, retrieve scores the relevance component of memory retrieval by cosine similarity in embedding space; when they are not, it falls back to a lexical overlap score, so a model still runs end-to-end without an embedding endpoint.\n\n\n\n\n\n","category":"function"},{"location":"api/cognition/#GABM.supports_embeddings","page":"Cognition and LLMs","title":"GABM.supports_embeddings","text":"supports_embeddings(llm) -> Bool\n\nWhether the backend llm implements embed. Defaults to false; backends that provide embeddings override it.\n\n\n\n\n\n","category":"function"},{"location":"api/cognition/#GABM.LLMError","page":"Cognition and LLMs","title":"GABM.LLMError","text":"LLMError(msg)\n\nException raised when a language-model backend fails — a network error from a live provider, or an exhausted response queue in a ScriptedLLM.\n\n\n\n\n\n","category":"type"},{"location":"api/cognition/#GABM.PromptingToolsLLM","page":"Cognition and LLMs","title":"GABM.PromptingToolsLLM","text":"PromptingToolsLLM(; model, embedding_model, temperature, max_tokens)\n\nA live language-model backend that forwards to PromptingTools.jl.\n\nBecause PromptingTools.jl is itself provider-agnostic, a PromptingToolsLLM can drive OpenAI, Anthropic, Mistral, Ollama, or a locally hosted model — the choice is made entirely by the model string and the API keys present in the environment (see the PromptingTools.jl documentation for key configuration).\n\nKeyword arguments\n\nmodel::String = \"claudeh\": the chat model, given as a PromptingTools.jl model name or registered alias (e.g. \"claudeh\", \"claudes\", \"gpt4om\").\nembedding_model::String = \"text-embedding-3-small\": the model used by embed.\ntemperature::Float64 = 0.7: default sampling temperature.\nmax_tokens::Int = 1024: maximum tokens per completion.\n\nExample\n\nllm = PromptingToolsLLM(model = \"claudeh\", temperature = 0.9)\ncomplete(llm, \"In one sentence, describe a quiet morning.\")\n\n\n\n\n\n","category":"type"},{"location":"api/cognition/#GABM.ScriptedLLM","page":"Cognition and LLMs","title":"GABM.ScriptedLLM","text":"ScriptedLLM(responses::AbstractVector)\nScriptedLLM(responder::Function)\nScriptedLLM(response::AbstractString)\n\nA deterministic mock backend. ScriptedLLM never touches the network, which makes it the right backend for unit tests, documentation examples, and any experiment that must be exactly reproducible.\n\nThree construction modes are supported:\n\nQueue. ScriptedLLM([\"yes\", \"no\"]) returns the responses in order, one per call to complete, and raises LLMError once the queue is exhausted — a missing response is therefore a loud failure, not a silent one.\nResponder. ScriptedLLM(prompt -> uppercase(prompt)) computes each response from the prompt, which is useful for mocks that must react to what the simulation actually asked.\nConstant. ScriptedLLM(\"ok\") returns the same string for every call.\n\nEvery prompt the backend receives is appended to llm.log, so a test can assert on exactly what the cognitive layer asked the model.\n\n\n\n\n\n","category":"type"},{"location":"api/cognition/#GABM.reset!","page":"Cognition and LLMs","title":"GABM.reset!","text":"reset!(llm::ScriptedLLM) -> ScriptedLLM\n\nRewind a queue-backed ScriptedLLM to its first response and clear its prompt log. Useful for re-running a deterministic scenario.\n\n\n\n\n\n","category":"function"},{"location":"api/cognition/#GABM.rate_importance","page":"Cognition and LLMs","title":"GABM.rate_importance","text":"rate_importance(llm, content) -> Float64\n\nAsk the backend llm to rate the poignancy of a memory on the Park et al. 1–10 scale, where 1 is wholly mundane (brushing one's teeth) and 10 is profoundly significant (a bereavement, a break-up).\n\nThe rating is the importance component of memory retrieval. A reply that contains no number falls back to a neutral 5.0.\n\n\n\n\n\n","category":"function"},{"location":"api/cognition/#GABM.observe!","page":"Cognition and LLMs","title":"GABM.observe!","text":"observe!(mind, content; tick, importance = nothing, llm = nothing,\n         kind = :observation) -> Int\n\nRecord that mind perceived content, and return the new memory's index.\n\nIf importance is given it is used directly. If it is nothing and a backend llm is supplied, the importance is rated by rate_importance; otherwise it defaults to a neutral 5.0.\n\nobserve! also accepts an agent in place of a Mind, operating on its mind.\n\n\n\n\n\n","category":"function"},{"location":"api/cognition/#GABM.recall","page":"Cognition and LLMs","title":"GABM.recall","text":"recall(mind, query; tick, n = 5, kinds = nothing, llm = nothing) -> Vector{MemoryEntry}\n\nReturn the memories of mind most relevant to query.\n\nrecall is the Mind-level wrapper around retrieve; see that function for how memories are scored. It also accepts an agent in place of a Mind.\n\n\n\n\n\n","category":"function"},{"location":"api/cognition/#GABM.reflect!","page":"Cognition and LLMs","title":"GABM.reflect!","text":"reflect!(mind, llm; tick, n_recent = 25, n_questions = 3, n_evidence = 8,\n         rate = false) -> Vector{String}\n\nSynthesise higher-level insights from recent experience and store them as :reflection memories. Returns the text of the new reflections.\n\nThis is the reflection step of Park et al. (2023), run in three stages:\n\nThe most recent n_recent memories are shown to llm, which is asked for the n_questions most salient questions they raise.\nFor each question, retrieve gathers up to n_evidence supporting memories.\nllm is asked to infer one high-level insight from that evidence; the insight is stored with citations pointing back to it.\n\nBecause reflections are themselves memories, later reflections can build on earlier ones, giving the agent a deepening self-model over a long run. With rate = true each reflection's importance is scored by rate_importance rather than fixed at 8.0.\n\n\n\n\n\n","category":"function"},{"location":"api/cognition/#GABM.maybe_reflect!","page":"Cognition and LLMs","title":"GABM.maybe_reflect!","text":"maybe_reflect!(mind, llm; tick, threshold = 100.0, kwargs...) -> Bool\n\nTrigger reflect! only when enough has happened to warrant it, and report whether it fired.\n\nThe summed importance of every :observation formed since the agent's last reflection is compared with threshold; if it is exceeded, reflect! runs and true is returned. This is the Park et al. trigger that keeps reflection periodic rather than constant, and it is what the default generative_step! uses. Extra keyword arguments are forwarded to reflect!.\n\n\n\n\n\n","category":"function"},{"location":"api/cognition/#GABM.plan!","page":"Cognition and LLMs","title":"GABM.plan!","text":"plan!(mind, llm; tick, horizon = \"the day ahead\", context = \"\",\n      n_steps = 5) -> Vector{String}\n\nHave the agent draw up a plan, store it on mind.plan, record it as a :plan memory, and return it as a list of steps.\n\nThe prompt combines the agent's persona, its current status, the context string supplied by the model, and the reflections most relevant to horizon, so the plan is grounded in what the agent has already concluded about itself. n_steps bounds the plan's length.\n\n\n\n\n\n","category":"function"},{"location":"api/cognition/#GABM.decide","page":"Cognition and LLMs","title":"GABM.decide","text":"decide(mind, llm, situation; tick, options = String[],\n       remember_situation = true, n_memories = 6) -> Decision\n\nHave the agent decide what to do in situation, and return a Decision.\n\nThis is the act step of the cognitive loop. It assembles a prompt from the agent's persona, status, current plan, and the n_memories memories most relevant to situation (via retrieve), then asks llm for an action and a one-line reason.\n\nIf options is non-empty the agent is constrained to choose one of them, which is what makes decide slot cleanly into a classical agent-based model: the discrete choice set is the model's, the selection is the language model's. When remember_situation is true the situation is also stored as an observation, so the agent will recall having faced it.\n\ndecide also accepts an agent in place of a Mind.\n\n\n\n\n\n","category":"function"},{"location":"api/cognition/#GABM.Decision","page":"Cognition and LLMs","title":"GABM.Decision","text":"Decision(action, reasoning, raw)\n\nThe result of a decide call.\n\naction::String — what the agent does, as a short imperative phrase.\nreasoning::String — the agent's stated justification, if it gave one.\nraw::String — the model's unparsed reply, kept for debugging and logging.\n\n\n\n\n\n","category":"type"},{"location":"api/cognition/#GABM.converse","page":"Cognition and LLMs","title":"GABM.converse","text":"converse(a, b, llm; topic = \"\", turns = 4, tick, remember = true) -> Vector{Pair{String,String}}\n\nGenerate a turn-taking dialogue between two minds and return it as a list of name => utterance pairs.\n\na and b may be Minds or agents. The two speak alternately for turns utterances each; every utterance is produced by llm from the speaker's persona, the memories most relevant to the conversation, and the transcript so far. When remember = true a summary of the dialogue is written back into both minds as an observation, so the conversation becomes part of each agent's history.\n\n\n\n\n\n","category":"function"},{"location":"#GABM.jl","page":"Home","title":"GABM.jl","text":"Generative agent-based modeling for Julia.\n\nAn agent-based model is a population of interacting decision-makers, simulated forward in time to see what collective behaviour their individual rules produce. For fifty years those rules have been written as equations or if-then logic: an agent compares two payoffs, samples a probability, follows a threshold. A generative agent-based model replaces that fixed logic with a language model. The agent is given a natural-language identity, accumulates natural-language memories of what happens to it, and — when the time comes to act — is asked, in natural language, what it would do.\n\nGABM.jl is the machinery that makes this practical in Julia. It supplies the cognitive architecture — memory, retrieval, reflection, planning, decision — and couples it to Agents.jl for everything a simulation also needs: scheduling, spaces, data collection, reproducibility.","category":"section"},{"location":"#Two-kinds-of-agent","page":"Home","title":"Two kinds of agent","text":"The contrast is sharpest in an example. Consider an agent deciding whether to cooperate in a repeated social dilemma.\n\nA classical agent decides with a rule the modeller wrote down:\n\ndecision = payoff(:cooperate, neighbours) > payoff(:defect, neighbours) ?\n           :cooperate : :defect\n\nThe rule is transparent, fast, and exactly reproducible. It is also exactly as rich as the modeller made it: the agent knows nothing the equation does not encode, remembers nothing, and cannot be surprising.\n\nA generative agent decides by consulting a language model that has been told who the agent is and what it has lived through:\n\nusing GABM\n\nmind = Mind(Persona(\"Wbishere\";\n    occupation = \"smallholder farmer\",\n    traits = [\"cautious\", \"values reputation\", \"long memory\"]))\n\nobserve!(mind, \"My neighbour Adsila took water from the shared channel \" *\n               \"without asking during the drought.\"; tick = 12)\n\ndecision = decide(mind, llm,\n    \"The irrigation council asks whether to keep sharing water with Adsila.\";\n    tick = 30, options = [\"keep sharing\", \"cut Adsila off\"])\n\ndecision.action     # \"cut Adsila off\"\ndecision.reasoning  # \"Adsila broke the channel agreement during the drought.\"\n\nThe generative agent's choice is grounded in a specific remembered grievance that no payoff matrix contained. It is slower, costs an API call, and is only statistically reproducible — but it can express reciprocity, reputation, and narrative memory without any of those being formalised in advance.\n\nGABM.jl does not argue that one kind of agent is better. It makes the second kind a first-class citizen of an Agents.jl model, so the two can be mixed, compared, and validated against each other.","category":"section"},{"location":"#The-architecture:-a-memory-stream-and-four-operations","page":"Home","title":"The architecture: a memory stream and four operations","text":"GABM.jl implements the cognitive architecture of Park et al. (2023), Generative Agents: Interactive Simulacra of Human Behavior. Its one essential data structure is the memory stream: an append-only, timestamped log of everything the agent has perceived, every conclusion it has drawn, and every plan it has formed. Cognition is then four operations that read and write that stream.\n\n                       ┌──────────────────────────────┐\n            perceive   │                              │\n   world ────────────► │   observe!   →   :observation │\n                       │                              │\n                       │   reflect!   →   :reflection  │  ── synthesise\n                       │      ▲                        │     insight from\n                       │      │  retrieve              │     past memories\n                       │   ┌──┴────────────────────┐   │\n                       │   │     MEMORY STREAM      │   │\n                       │   └──┬────────────────────┘   │\n                       │      │  retrieve              │\n                       │   plan!      →   :plan        │  ── form intentions\n                       │                              │\n                       │   decide     →   action ─────┼──────────► world\n                       │                              │\n                       └──────────────────────────────┘\n\nobserve! writes a perception into the stream as an :observation, scoring how poignant it is on a 1–10 scale.\nreflect! reads recent memories, asks the language model what high-level questions they raise, answers each from retrieved evidence, and writes the answers back as :reflection memories. Reflections are memories, so later reflections compound earlier ones.\nplan! reads the agent's reflections and writes a :plan.\ndecide reads the memories most relevant to the current situation and returns an action.\n\nThe hard problem common to all four is retrieval. After a long run an agent remembers far more than fits in a single prompt, so each operation must select the few memories that matter now.","category":"section"},{"location":"#Retrieving-the-right-memories","page":"Home","title":"Retrieving the right memories","text":"GABM.jl scores every candidate memory on the three signals of Park et al. (2023) and returns the highest-scoring few. For a query q issued at tick t, a memory m receives\n\ntextscore(m mid q t) =\n   w_textrecwidetilder(mt) +\n   w_textimpwidetildep(m)   +\n   w_textrelwidetildes(mq)\n\na weighted sum of three components, each min–max normalised across the candidate set so that the weights w are comparable.\n\nRecency decays exponentially in the time since the memory was last used:\n\nr(mt) = delta^t - a(m)\n\nwhere a(m) is the memory's last-accessed tick and delta in (01) is the per-tick decay base. Because retrieving a memory resets a(m) to the current tick, attention is self-reinforcing — a memory recalled today is easier to recall tomorrow.\n\nImportance is the memory's stored poignancy p(m) in 110, the rating rate_importance elicits from the language model when the memory is formed. A mundane observation (the weather) scores low; a consequential one (a betrayal) scores high and resists being crowded out by sheer recency.\n\nRelevance s(mq) measures how much the memory bears on the query. When embeddings are available it is the cosine similarity of the memory and query embeddings; otherwise GABM.jl falls back to lexical token overlap, so a model still runs end-to-end without an embedding endpoint.\n\nretrieve computes all three, and the Memory Stream guide works the formula through on a concrete stream.","category":"section"},{"location":"#Generative-cognition-on-an-Agents.jl-model","page":"Home","title":"Generative cognition on an Agents.jl model","text":"GABM.jl is deliberately not a simulation engine. Scheduling agents, laying them out in space, stepping the clock, collecting data into a DataFrame, seeding the random number generator for reproducibility — Agents.jl already does all of this, and does it well. GABM.jl supplies only the layer Agents.jl lacks: a mind.\n\n   ┌─────────────────────────┐        ┌────────────────────────────┐\n   │        Agents.jl        │        │           GABM.jl          │\n   │                         │        │                            │\n   │  StandardABM            │        │  Persona      — identity    │\n   │  spaces (grid, graph,   │   +    │  MemoryStream — experience  │\n   │     continuous)         │        │  observe! / reflect! /      │\n   │  scheduler, run!, step! │        │     plan! / decide          │\n   │  data collection        │        │  AbstractLLM  — the backend │\n   └─────────────────────────┘        └────────────────────────────┘\n                    │                              │\n                    └──────────────┬───────────────┘\n                                   ▼\n                       a generative agent-based model\n\nA generative model is an ordinary Agents.jl StandardABM. generative_abm builds one and guarantees it carries the two things the cognitive layer needs: a shared language-model backend and a tick counter. Each agent is an Agents.jl agent that owns a Mind; the ready-made GenerativeAgent covers non-spatial models, and any @agent type with a mind field works for spatial ones. The modeller writes a normal agent_step! — the only difference is that, inside it, the agent thinks:\n\nfunction agent_step!(agent, model)\n    observe!(agent, perceive(agent, model); tick = abmclock(model))\n    d = decide(agent, abmllm(model), situation(agent, model);\n               tick = abmclock(model))\n    apply!(agent, model, d.action)\nend\n\nThe Building Models guide develops this skeleton into a complete, runnable simulation.","category":"section"},{"location":"#Live-and-scripted-backends","page":"Home","title":"Live and scripted backends","text":"Every cognitive call ultimately reaches an AbstractLLM backend, and GABM.jl ships two.\n\nPromptingToolsLLM is the live backend. It forwards to PromptingTools.jl and through it to any provider that package supports — Anthropic, OpenAI, Mistral, or a model served locally by Ollama. Switching providers is a one-line change.\n\nScriptedLLM is a deterministic mock. It returns canned or prompt-computed responses without ever touching the network, which makes it the right backend for unit tests, for the examples in this manual, and for any experiment whose results must be bit-for-bit reproducible. Because the whole simulation is written against AbstractLLM, the same model runs unchanged on either backend — developed and tested against ScriptedLLM, then run for real against PromptingToolsLLM.","category":"section"},{"location":"#When-a-generative-model-earns-its-cost","page":"Home","title":"When a generative model earns its cost","text":"Generative agents are slower and dearer than equations, and their output is a distribution rather than a number. They repay that cost when a model needs:\n\nheterogeneous, legible agents — populations specified as written personas rather than parameter vectors;\nmemory and narrative — behaviour that depends on a particular remembered history, not just an aggregate state variable;\nopen-ended action — choices the modeller did not enumerate in advance;\nnatural-language environments — settings (negotiations, deliberations, rumour spread) whose very content is text.\n\nWhen agents are interchangeable and their decision rule is genuinely a short equation, a classical Agents.jl model remains the better tool — and GABM.jl leaves it untouched, ready to host generative agents alongside it whenever the question calls for them.","category":"section"},{"location":"#Notation-used-in-this-manual","page":"Home","title":"Notation used in this manual","text":"Symbol Meaning\nt The current simulation tick (an integer clock)\nm A single memory in an agent's stream\nq A retrieval query (the current situation or question)\na(m) The tick at which memory m was last retrieved\ndelta Per-tick recency decay base, delta in (01)\nrps Raw recency, importance, and relevance scores\nwidetildecdot A score after min–max normalisation to 01\nw_textrecw_textimpw_textrel Retrieval weights on the three components","category":"section"},{"location":"#Where-to-next","page":"Home","title":"Where to next","text":"Getting Started — install the package and run a complete generative model offline, with no API key.\nLanguage Models — the AbstractLLM interface, the live and scripted backends, and how to choose between them.\nThe Memory Stream — MemoryStream and the retrieve scoring formula, worked through on a concrete example.\nThe Cognitive Loop — observe!, reflect!, plan!, and decide in detail.\nBuilding Models — wiring the cognitive loop into an Agents.jl StandardABM and running it.\nReferences — annotated bibliography of the generative-agent and agent-based-modelling literature this package builds on.","category":"section"}]
}
